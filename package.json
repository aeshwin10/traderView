{
  "name": "stock-broker-dashboard",
  "version": "1.0.0",
  "description": "Real-time Stock Broker Client Dashboard with JWT auth and WebSocket updates",
  "scripts": {
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run start",
    "build": "cd server && npm run build && cd ../client && npm run build",
    "test": "cd server && npm test"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": ["stock", "dashboard", "websocket", "react", "express", "typescript"],
  "author": "Stock Dashboard Team",
  "license": "MIT"
} 